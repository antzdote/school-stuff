#include <iostream>
#include <cmath>
using namespace std;
int main()
{
    //declare constants
    const double pie =3.142;
    const double g= 9.8;
    float S, SO, VO, t, G, r, a, p, M1, M2, FV, PV, C, A, b, Y;
    int yrs;
    S= SO +VO + (1/2*8*pow(t,2));
    G = 4*pie*r*r(a*a*a/((p*p)*(M1+M2));
    G = 4*pie*pow(r,2)*pow(a,3)/(pow(p,2)*(M1+M2));
    C= (pow(a,2)+pow(b,2)-2*a*bcos(y));


    return 0;
}

